server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA (React Router) : toute route inconnue de nginx retombe sur
    # index.html, le routing côté client prend le relais.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
